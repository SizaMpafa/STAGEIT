default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: stageit_development

test:
  <<: *default
  database: stageit_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
